export enum Stream {
  Authenticate = 'authenticate',
  Listen = 'listen',
  Authorization = 'authorization',
  Authorized = 'authorized',
  Unauthorized = 'unauthorized',
  Listening = 'listening',
}
